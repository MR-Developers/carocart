import 'package:flutter/material.dart';

class DelivaryParterHome extends StatelessWidget {
  const DelivaryParterHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Welcome to Delivery Partner Home Screen",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
